package com.example.setdrive.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais Modo Claro
val PrimaryColor = Color(0xFF6201ED) // Roxo
val SecondaryColor = Color(0xFF888484) //
val TertiaryColor = Color(0xFF017006) //

// Cores principais Modo Escuro
val PrimaryColorDark = Color(0xFF624DB9) // Roxo
val SecondaryColorDark = Color(0xFF919194) //
val TertiaryColorDark = Color(0xFF10B926) //

// Fundo e Superfícies para Modo Claro
val BackgroundColor = Color(0xFFFFFFFF) // Branco
val SurfaceColor = Color(0xFFE9E8EF) // Cinza claro

// Fundo e Superfícies para Modo Escuro
val BackgroundColorDark = Color(0xFF131315) // Preto fosco
val SurfaceColorDark = Color(0xFF262525) // Cinza escuro

// Texto para Modo Claro
val OnPrimaryColor = Color(0xFFFFFFFF) // Branco em fundo primário
val OnSecondaryColor = Color(0xFFFFFFFF) // Branco em fundo secundário
val OnTertiaryColor = Color(0xFFFFFFFF) // Branco em fundo terciário
val OnBackgroundColorLight = Color(0xFF000000) // Preto para fundos claros
val OnSurfaceColorLight = Color(0xFF282727) // Cinza escuro para superfícies claras

// Texto para Modo Escuro
val OnPrimaryColorDark = Color(0xFFEEEFF1) // Cinza claro em fundo primário
val OnSecondaryColorDark = Color(0xFFEEEFF1) // Cinza claro em fundo secundário
val OnTertiaryColorDark = Color(0xFFEEEFF1) // Cinza claro em fundo terciário
val OnBackgroundColorDark = Color(0xFFE0E0E0) // Cinza claro para fundos escuros
val OnSurfaceColorDark = Color(0xFFCECBCB) // Cinza claro para superfícies escuras
